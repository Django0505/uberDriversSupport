<!--This is the handlebars page for the home page with information about nelisa and her shop-->
<div id="page-wrapper" >
  <ul class="nav nav-tabs">
    <li role="presentation"><a href="/queries/new">New</a></li>
    <li role="presentation"><a href="/queries/clear">Clear</a></li>
  </ul>

  <div class="list-group">
    {{#queries}}
    <a href="/queries/view/{{this.query_id}}" class="queryBox{{this.query_id}} list-group-item">
      <strong><h4>Agent: </h4><h5 class="agentName{{this.query_id}}"></h5></strong>{{this.username}}
    </br>
      <strong>Query: </strong>{{this.query}}
      <span class="badge">{{this.status}}</span>
    </a>
    {{/queries}}
  </div>
</div>
<script src="/socket.io/socket.io.js"></script>
<script>
  var socket = io();


  socket.on('query_accepted', function(evt){
    //alert("go to agent : " + evt.agent );

    $(".queryBox"+evt.query_id).css({
                                       'background-color' : 'green',
                                       'color' : '#fff'
                                  });
    $(".agentName"+evt.query_id).text(evt.agent);
    $(".badge").append("</br><a href='/agent/rate'>Rate Agent</a>")
    if (username === evt.username){

      //alert("go to agent : " + evt.agent );

    }

  });

</script>
